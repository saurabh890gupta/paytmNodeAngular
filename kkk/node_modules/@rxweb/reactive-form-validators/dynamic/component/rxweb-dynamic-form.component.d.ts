import { QueryList } from "@angular/core";
import { ControlTemplateDirective } from '../directives/control-template.directive';
export declare class RxwebDynamicFormComponent {
    private uiBindings;
    private _controlTemplates;
    private _viewMode;
    private formRow;
    private isChangedMode;
    sectionsConfig: {
        [key: string]: any;
    };
    uiFramework: string;
    viewMode: string;
    controlsConfig: any;
    bindings: string[];
    controlTemplates: QueryList<ControlTemplateDirective>;
    designRows(bindings: string[]): any[];
    changeSectionViewMode(): void;
}
