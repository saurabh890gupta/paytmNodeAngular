import { OnDestroy, QueryList, ComponentFactoryResolver, ViewContainerRef, OnInit } from "@angular/core";
import { FormGroup, FormArray } from "@angular/forms";
import { FormControlConfig } from '../form-control-config';
import { ControlTemplateDirective } from '../directives/control-template.directive';
export declare class RxWebControlComponent implements OnInit, OnDestroy {
    componentFactoryResolver: ComponentFactoryResolver;
    controlContainerRef: ViewContainerRef;
    formControlConfig: FormControlConfig;
    sectionConfig: any;
    viewMode: string;
    name: string;
    formGroup: FormGroup;
    formArray: FormArray;
    controlsConfig: any;
    controlTemplates: QueryList<ControlTemplateDirective>;
    private componentRef;
    private isCleared;
    private componentConfig;
    private eventSubscriptions;
    private _viewMode;
    constructor(componentFactoryResolver: ComponentFactoryResolver);
    ngOnInit(): void;
    process(): void;
    private getName;
    private createInstance;
    private subscribeEvents;
    private setSectionConfigParams;
    private setFieldConfigParams;
    overrideValueProp(): void;
    onHide: () => void;
    private clear;
    addParams(instance: any, propName: string, inputs: any): void;
    ngOnDestroy(): void;
}
