import { DynamicFormConfiguration } from "../models";
import { DynamicFormBuildConfig } from '../models/config/dynamic-form-build-config';
export declare class DynamicFormBuilder {
    private validatorBindings;
    private formConfiguration;
    constructor(validatorBindings: Function, formConfiguration: DynamicFormConfiguration);
    dynamicFormGroup(fields: any[], formConfiguration: DynamicFormConfiguration): DynamicFormBuildConfig;
    private completeModelConfig;
    private createFormArray;
    private getRefObject;
    private addTwoProp;
    private createDynamicFormGroup;
    private getDynamicModelInstance;
}
